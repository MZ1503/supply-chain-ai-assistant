
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    command: ["streamlit", "run", "app.py", "--server.port", "8501"]
    depends_on:
      - api